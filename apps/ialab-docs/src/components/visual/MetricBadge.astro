---
const {
  label,
  value,
  className = '',
} = Astro.props;
---

<div class={`ai-visual-code-badge ${className}`.trim()}>
  <span class="ai-visual-card-kicker">{label}</span>
  <span class="ai-visual-metric text-white">{value}</span>
</div>
